import RevenueWidget from './revenue-widget';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Revenue by Department · LEX' };

export default function RevenueWidgetPage() {
  return <RevenueWidget />;
}
